# Flutter三方库 stop_watch_timer 适配 OpenHarmony —— 实现倒计时

# 目录

- [前言](#前言)
- [功能代码实现](#功能代码实现)
- [本次开发中容易遇到的问题](#本次开发中容易遇到的问题)
- [总结本次开发中用到的技术点](#总结本次开发中用到的技术点)

欢迎加入开源鸿蒙跨平台社区： https://openharmonycrossplatform.csdn.net

# 前言
在移动应用开发中，倒计时功能是一个常见且实用的需求，无论是用于活动倒计时、任务计时还是游戏环节，都能为用户提供清晰的时间参考。本文将详细介绍如何在 OpenHarmony 平台上，利用 Flutter 三方库 stop_watch_timer 实现一个功能完整、交互友好的倒计时组件。

随着跨平台开发技术的不断发展，Flutter 已成为构建多端应用的重要选择。而 OpenHarmony 作为全新的分布式操作系统，为应用提供了更广阔的发展空间。将 Flutter 应用适配到 OpenHarmony 平台，不仅能扩大应用的覆盖范围，还能充分利用两个生态的优势。

本文将从实际项目出发，详细讲解如何搭建开发环境、集成 stop_watch_timer 库、实现倒计时组件，并解决开发过程中遇到的常见问题，为开发者提供一份全面的实战指南。

# 功能代码实现

## 1. 项目依赖配置

首先，我们需要在 `pubspec.yaml` 文件中添加 stop_watch_timer 依赖，确保项目能够使用该库的功能：

```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # 其他依赖
  cupertino_icons: ^1.0.8
  # 倒计时功能依赖
  stop_watch_timer: ^2.0.0
```

添加依赖后，执行 `flutter pub get` 命令来获取依赖包。

## 2. 倒计时组件开发

我们创建了 `countdown_timer_component.dart` 文件，实现了一个功能完整的倒计时组件：

### 2.1 组件结构设计

该组件采用 StatefulWidget 实现，包含以下核心功能：
- 倒计时显示
- 时长设置
- 开始/暂停/继续/重置控制
- 常用时长快速选择

### 2.2 核心代码实现

```dart
import 'package:flutter/material.dart';
import 'package:stop_watch_timer/stop_watch_timer.dart';

class CountdownTimerComponent extends StatefulWidget {
  const CountdownTimerComponent({super.key});

  @override
  State<CountdownTimerComponent> createState() => _CountdownTimerComponentState();
}

class _CountdownTimerComponentState extends State<CountdownTimerComponent> {
  // 初始化倒计时器，设置为倒计时模式
  final StopWatchTimer _stopWatchTimer = StopWatchTimer(
    mode: StopWatchMode.countDown,
  );

  // 时长控制器，默认60秒
  final TextEditingController _durationController = TextEditingController(text: '60');
  int _duration = 60; // 默认60秒
  bool _isRunning = false;
  bool _isPaused = false;

  @override
  void initState() {
    super.initState();
    // 设置初始时长
    _stopWatchTimer.setPresetTime(mSec: _duration * 1000);
  }

  @override
  void dispose() {
    // 释放资源
    _stopWatchTimer.dispose();
    _durationController.dispose();
    super.dispose();
  }

  // 开始倒计时
  void _startCountdown() {
    setState(() {
      _isRunning = true;
      _isPaused = false;
    });
    _stopWatchTimer.onStartTimer();
  }

  // 暂停倒计时
  void _pauseCountdown() {
    setState(() {
      _isPaused = true;
    });
    _stopWatchTimer.onStopTimer();
  }

  // 继续倒计时
  void _resumeCountdown() {
    setState(() {
      _isPaused = false;
    });
    _stopWatchTimer.onStartTimer();
  }

  // 重置倒计时
  void _resetCountdown() {
    setState(() {
      _isRunning = false;
      _isPaused = false;
    });
    _stopWatchTimer.setPresetTime(mSec: _duration * 1000);
    _stopWatchTimer.onResetTimer();
  }

  // 更新时长
  void _updateDuration() {
    final value = int.tryParse(_durationController.text);
    if (value != null && value > 0) {
      setState(() {
        _duration = value;
      });
      _stopWatchTimer.setPresetTime(mSec: _duration * 1000);
      _stopWatchTimer.onResetTimer();
      setState(() {
        _isRunning = false;
        _isPaused = false;
      });
    }
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(20),
      child: Card(
        elevation: 5,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(10),
        ),
        child: Padding(
          padding: const EdgeInsets.all(20),
          child: Column(
            crossAxisAlignment: CrossAxisAlignment.center,
            children: [
              // 标题
              const Text(
                '倒计时器',
                style: TextStyle(
                  fontSize: 24,
                  fontWeight: FontWeight.bold,
                  color: Colors.deepPurple,
                ),
              ),
              const SizedBox(height: 30),

              // 倒计时显示
              StreamBuilder<int>(
                stream: _stopWatchTimer.rawTime,
                initialData: _duration * 1000,
                builder: (context, snapshot) {
                  final value = snapshot.data!;
                  final displayTime = StopWatchTimer.getDisplayTime(value,
                      hours: false,
                      minute: true,
                      second: true,
                      milliSecond: false);
                  return Text(
                    displayTime,
                    style: const TextStyle(
                      fontSize: 48,
                      fontWeight: FontWeight.bold,
                      color: Colors.deepPurple,
                    ),
                  );
                },
              ),
              const SizedBox(height: 30),

              // 时长设置
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  const Text(
                    '时长（秒）：',
                    style: TextStyle(
                      fontSize: 16,
                    ),
                  ),
                  SizedBox(
                    width: 80,
                    child: TextField(
                      controller: _durationController,
                      keyboardType: TextInputType.number,
                      decoration: const InputDecoration(
                        border: OutlineInputBorder(),
                      ),
                      onChanged: (value) {
                        // 实时更新时长
                        final intValue = int.tryParse(value);
                        if (intValue != null && intValue > 0) {
                          _duration = intValue;
                        }
                      },
                    ),
                  ),
                  const SizedBox(width: 10),
                  ElevatedButton(
                    onPressed: _updateDuration,
                    style: ElevatedButton.styleFrom(
                      backgroundColor: Colors.deepPurple,
                    ),
                    child: const Text('设置'),
                  ),
                ],
              ),
              const SizedBox(height: 30),

              // 控制按钮
              Row(
                mainAxisAlignment: MainAxisAlignment.center,
                children: [
                  if (!_isRunning)
                    ElevatedButton(
                      onPressed: _startCountdown,
                      style: ElevatedButton.styleFrom(
                        backgroundColor: Colors.green,
                        padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
                      ),
                      child: const Text(
                        '开始',
                        style: TextStyle(
                          fontSize: 16,
                          color: Colors.white,
                        ),
                      ),
                    ),
                  if (_isRunning && !_isPaused)
                    ElevatedButton(
                      onPressed: _pauseCountdown,
                      style: ElevatedButton.styleFrom(
                        backgroundColor: Colors.orange,
                        padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
                      ),
                      child: const Text(
                        '暂停',
                        style: TextStyle(
                          fontSize: 16,
                          color: Colors.white,
                        ),
                      ),
                    ),
                  if (_isPaused)
                    ElevatedButton(
                      onPressed: _resumeCountdown,
                      style: ElevatedButton.styleFrom(
                        backgroundColor: Colors.blue,
                        padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
                      ),
                      child: const Text(
                        '继续',
                        style: TextStyle(
                          fontSize: 16,
                          color: Colors.white,
                        ),
                      ),
                    ),
                  const SizedBox(width: 10),
                  ElevatedButton(
                    onPressed: _resetCountdown,
                    style: ElevatedButton.styleFrom(
                      backgroundColor: Colors.red,
                      padding: const EdgeInsets.symmetric(horizontal: 30, vertical: 15),
                    ),
                    child: const Text(
                      '重置',
                      style: TextStyle(
                        fontSize: 16,
                        color: Colors.white,
                      ),
                    ),
                  ),
                ],
              ),
              const SizedBox(height: 30),

              // 状态提示
              Text(
                _isRunning
                    ? _isPaused
                        ? '已暂停'
                        : '倒计时中...'
                    : '准备就绪',
                style: TextStyle(
                  fontSize: 16,
                  color: _isRunning
                      ? _isPaused
                          ? Colors.orange
                          : Colors.green
                      : Colors.grey,
                  fontWeight: FontWeight.bold,
                ),
              ),
              const SizedBox(height: 20),

              // 常用时长
              const Text(
                '常用时长：',
                style: TextStyle(
                  fontSize: 16,
                  fontWeight: FontWeight.bold,
                ),
              ),
              const SizedBox(height: 10),
              Wrap(
                spacing: 10,
                runSpacing: 10,
                children: [
                  _buildDurationChip('10秒', 10),
                  _buildDurationChip('30秒', 30),
                  _buildDurationChip('60秒', 60),
                  _buildDurationChip('120秒', 120),
                  _buildDurationChip('300秒', 300),
                ],
              ),
            ],
          ),
        ),
      ),
    );
  }

  // 构建时长选择芯片
  Widget _buildDurationChip(String label, int seconds) {
    return GestureDetector(
      onTap: () {
        setState(() {
          _duration = seconds;
          _durationController.text = seconds.toString();
          _stopWatchTimer.setPresetTime(mSec: seconds * 1000);
          _stopWatchTimer.onResetTimer();
          _isRunning = false;
          _isPaused = false;
        });
      },
      child: Chip(
        label: Text(label),
        backgroundColor: Colors.blue[100],
      ),
    );
  }
}
```

### 2.3 组件使用方法

在 `main.dart` 文件中，我们直接将 CountdownTimerComponent 集成到首页：

```dart
import 'package:flutter/material.dart';
import 'components/countdown_timer_component.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter for openHarmony',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
        useMaterial3: true,
      ),
      debugShowCheckedModeBanner: false,
      home: const MyHomePage(title: 'Flutter for openHarmony'),
    );
  }
}

class MyHomePage extends StatefulWidget {
  const MyHomePage({super.key, required this.title});

  final String title;

  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text(widget.title),
        backgroundColor: Colors.deepPurple,
      ),
      body: SingleChildScrollView(
        child: Column(
          children: [
            const SizedBox(height: 20),
            const Text(
              'Flutter三方库 stop_watch_timer 适配 OpenHarmony',
              style: TextStyle(
                fontSize: 20,
                fontWeight: FontWeight.bold,
                color: Colors.deepPurple,
              ),
            ),
            const SizedBox(height: 10),
            const Text(
              '实现倒计时',
              style: TextStyle(
                fontSize: 16,
                color: Colors.grey,
              ),
            ),
            const SizedBox(height: 20),
            // 直接集成倒计时组件
            const CountdownTimerComponent(),
          ],
        ),
      ),
    );
  }
}
```

## 3. 开发注意事项

1. **依赖版本选择**：
   - 使用 stop_watch_timer ^2.0.0 版本，该版本提供了更清晰的 API 接口
   - 注意不同版本的方法名称可能不同，需要根据实际版本调整代码

2. **资源管理**：
   - 务必在组件销毁时调用 `dispose()` 方法释放计时器资源
   - 避免内存泄漏和不必要的资源消耗

3. **状态管理**：
   - 使用 StreamBuilder 监听计时器状态变化
   - 保持 UI 与数据状态的同步

4. **用户体验**：
   - 提供清晰的视觉反馈，如不同状态下的按钮颜色变化
   - 支持常用时长快速选择，提升用户操作效率

5. **错误处理**：
   - 对用户输入的时长进行有效性检查
   - 确保输入的是正整数，避免出现无效时长

# 本次开发中容易遇到的问题

## 1. 方法名称不匹配问题

在使用 stop_watch_timer 库时，最容易遇到的问题是方法名称不匹配。不同版本的库可能使用不同的方法名称，例如：

- **错误示例**：使用 `_stopWatchTimer.start()`、`_stopWatchTimer.stop()`、`_stopWatchTimer.reset()`
- **正确示例**：使用 `_stopWatchTimer.onStartTimer()`、`_stopWatchTimer.onStopTimer()`、`_stopWatchTimer.onResetTimer()`

**解决方案**：
- 查看库的官方文档或源码，确认正确的方法名称
- 注意版本兼容性，不同版本的 API 可能有所不同

## 2. 依赖版本问题

使用不兼容的依赖版本可能导致编译错误或运行时异常。

**解决方案**：
- 在 `pubspec.yaml` 中指定明确的版本号，如 `stop_watch_timer: ^2.0.0`
- 执行 `flutter pub get` 确保依赖正确安装
- 定期更新依赖，保持与最新版本的兼容性

## 3. 资源泄漏问题

如果不正确释放计时器资源，可能导致内存泄漏。

**解决方案**：
- 在 `dispose()` 方法中调用 `_stopWatchTimer.dispose()`
- 同时释放其他控制器资源，如 `TextEditingController`

## 4. 状态管理问题

倒计时过程中的状态管理不当可能导致 UI 显示异常。

**解决方案**：
- 使用 `StreamBuilder` 正确监听计时器状态变化
- 确保 UI 状态与计时器状态保持同步
- 使用 `setState()` 及时更新 UI

## 5. 平台适配问题

在 OpenHarmony 平台上，可能会遇到一些平台特定的问题。

**解决方案**：
- 测试不同平台上的表现
- 针对平台差异进行适当的适配
- 遵循 OpenHarmony 的开发规范

# 总结本次开发中用到的技术点

## 1. Flutter 组件化开发

- **StatefulWidget**：使用有状态组件实现倒计时功能，管理组件的状态变化
- **StatelessWidget**：使用无状态组件构建应用结构和布局
- **组件封装**：将倒计时功能封装为独立组件，提高代码复用性和可维护性

## 2. 状态管理

- **StreamBuilder**：利用流（Stream）监听计时器状态变化，实现实时 UI 更新
- **setState**：使用 setState 方法更新组件状态，触发 UI 重建
- **状态变量**：使用布尔变量管理倒计时的运行、暂停状态

## 3. 依赖管理

- **pubspec.yaml**：配置项目依赖，添加 stop_watch_timer 库
- **版本控制**：指定依赖版本，确保版本兼容性
- **依赖获取**：使用 `flutter pub get` 获取依赖包

## 4. 计时器实现

- **StopWatchTimer**：使用 stop_watch_timer 库实现倒计时功能
- **倒计时模式**：设置 `mode: StopWatchMode.countDown` 实现倒计时
- **时长设置**：使用 `setPresetTime` 方法设置倒计时时长
- **控制方法**：使用 `onStartTimer`、`onStopTimer`、`onResetTimer` 控制计时器

## 5. UI 设计与交互

- **布局组件**：使用 Container、Card、Column、Row 等组件构建界面
- **输入控件**：使用 TextField 实现时长输入
- **按钮控件**：使用 ElevatedButton 实现功能按钮
- **快速选择**：使用 Chip 和 GestureDetector 实现常用时长快速选择
- **响应式设计**：使用 SingleChildScrollView 确保在不同屏幕尺寸下的显示效果

## 6. 资源管理

- **资源释放**：在 `dispose()` 方法中释放计时器和控制器资源
- **内存管理**：避免内存泄漏，确保应用性能

## 7. 平台适配

- **跨平台开发**：使用 Flutter 实现跨平台应用
- **OpenHarmony 适配**：确保应用在 OpenHarmony 平台上正常运行
- **平台兼容性**：考虑不同平台的特性和限制

## 8. 错误处理

- **输入验证**：对用户输入的时长进行有效性检查
- **异常处理**：处理可能出现的运行时异常

## 9. 用户体验优化

- **视觉反馈**：不同状态下的按钮颜色变化，提供清晰的视觉反馈
- **状态提示**：显示倒计时的当前状态，如"准备就绪"、"倒计时中"、"已暂停"
- **操作便捷性**：提供常用时长快速选择，减少用户输入

通过本次开发，我们成功实现了一个功能完整、交互友好的倒计时组件，展示了 Flutter 在 OpenHarmony 平台上的应用能力。同时，我们也掌握了一系列 Flutter 开发技巧，包括组件化开发、状态管理、依赖管理等，为后续的跨平台开发积累了宝贵经验。
