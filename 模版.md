# Flutter for OpenHarmony 实战：SizeTransition实现大小变化过渡

欢迎加入开源鸿蒙跨平台社区： https://openharmonycrossplatform.csdn.net

# 前言：跨生态开发的新机遇

在移动开发领域，我们总是面临着选择与适配。今天，你的Flutter应用在Android和iOS上跑得正欢，明天可能就需要考虑一个新的平台：HarmonyOS（鸿蒙）。这不是一道选答题，而是很多团队正在面对的现实。

Flutter的优势很明确——写一套代码，就能在两个主要平台上运行，开发体验流畅。而鸿蒙代表的是下一个时代的互联生态，它不仅仅是手机系统，更着眼于未来全场景的体验。将现有的Flutter应用适配到鸿蒙，听起来像是一个“跨界”任务，但它本质上是一次有价值的技术拓展：让产品触达更多用户，也让技术栈覆盖更广。

不过，这条路走起来并不像听起来那么简单。Flutter和鸿蒙，从底层的架构到上层的工具链，都有着各自的设计逻辑。会遇到一些具体的问题：代码如何组织？原有的功能在鸿蒙上如何实现？那些平台特有的能力该怎么调用？更实际的是，从编译打包到上架部署，整个流程都需要重新摸索。
这篇文章想做的，就是把这些我们趟过的路、踩过的坑，清晰地摊开给你看。我们不会只停留在“怎么做”，还会聊到“为什么得这么做”，以及“如果出了问题该往哪想”。这更像是一份实战笔记，源自真实的项目经验，聚焦于那些真正卡住过我们的环节。

无论你是在为一个成熟产品寻找新的落地平台，还是从一开始就希望构建能面向多端的应用，这里的思路和解决方案都能提供直接的参考。理解了两套体系之间的异同，掌握了关键的衔接技术，不仅能完成这次迁移，更能积累起应对未来技术变化的能力。

# 混合工程结构深度解析

项目目录架构

当Flutter项目集成鸿蒙支持后，典型的项目结构会发生显著变化。以下是经过ohos_flutter插件初始化后的项目结构：
```
my_flutter_harmony_app/
├── lib/                          # Flutter业务代码（基本不变）
│   ├── main.dart                 # 应用入口
│   ├── home_page.dart           # 首页
│   └── utils/
│       └── platform_utils.dart  # 平台工具类
├── pubspec.yaml                  # Flutter依赖配置
├── ohos/                         # 鸿蒙原生层（核心适配区）
│   ├── entry/                    # 主模块
│   │   └── src/main/
│   │       ├── ets/              # ArkTS代码
│   │       │   ├── MainAbility/
│   │       │   │   ├── MainAbility.ts       # 主Ability
│   │       │   │   └── MainAbilityContext.ts
│   │       │   └── pages/
│   │       │       ├── Index.ets           # 主页面
│   │       │       └── Splash.ets          # 启动页
│   │       ├── resources/        # 鸿蒙资源文件
│   │       │   ├── base/
│   │       │   │   ├── element/  # 字符串等
│   │       │   │   ├── media/    # 图片资源
│   │       │   │   └── profile/  # 配置文件
│   │       │   └── en_US/        # 英文资源
│   │       └── config.json       # 应用核心配置
│   ├── ohos_test/               # 测试模块
│   ├── build-profile.json5      # 构建配置
│   └── oh-package.json5         # 鸿蒙依赖管理
└── README.md
```
# 展示效果图片

flutter 实时预览 效果展示

运行到鸿蒙虚拟设备中效果展示

# 目录

- [前言：跨生态开发的新机遇](#前言：跨生态开发的新机遇)
- [混合工程结构深度解析](#混合工程结构深度解析)
- [展示效果图片](#展示效果图片)
- [功能代码实现](#功能代码实现)
  - [SizeTransitionWidget 组件开发](#SizeTransitionWidget-组件开发)
  - [首页集成实现](#首页集成实现)
- [开发中容易遇到的问题](#开发中容易遇到的问题)
- [总结开发中用到的技术点](#总结开发中用到的技术点)

# 功能代码实现

## SizeTransitionWidget 组件开发

### 组件设计思路

为实现大小变化过渡动画效果，我们创建了一个可复用的 `SizeTransitionWidget` 组件。该组件基于 Flutter 内置的 `SizeTransition` 组件，结合 `AnimationController` 实现了平滑的大小变化过渡效果，并封装了状态管理和交互逻辑，提供了更加灵活的配置选项。

### 核心代码实现

```dart
import 'package:flutter/material.dart';

class SizeTransitionWidget extends StatefulWidget {
  final Widget child;
  final Axis axis;
  final double axisAlignment;
  final Duration duration;
  final Curve curve;
  final Function()? onTap;

  const SizeTransitionWidget({
    Key? key,
    required this.child,
    this.axis = Axis.vertical,
    this.axisAlignment = 0.0,
    this.duration = const Duration(milliseconds: 300),
    this.curve = Curves.easeInOut,
    this.onTap,
  }) : super(key: key);

  @override
  State<SizeTransitionWidget> createState() => _SizeTransitionWidgetState();
}

class _SizeTransitionWidgetState extends State<SizeTransitionWidget> with SingleTickerProviderStateMixin {
  late AnimationController _controller;
  late Animation<double> _animation;
  bool _isExpanded = false;

  @override
  void initState() {
    super.initState();
    _controller = AnimationController(
      duration: widget.duration,
      vsync: this,
    )..value = 1.0; // 初始状态为展开
    _animation = CurvedAnimation(
      parent: _controller,
      curve: widget.curve,
    );
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  void _toggleSize() {
    setState(() {
      _isExpanded = !_isExpanded;
      if (_isExpanded) {
        _controller.forward();
      } else {
        _controller.reverse();
      }
    });
  }

  @override
  Widget build(BuildContext context) {
    return GestureDetector(
      onTap: () {
        _toggleSize();
        if (widget.onTap != null) {
          widget.onTap!();
        }
      },
      child: SizeTransition(
        sizeFactor: _animation,
        axis: widget.axis,
        axisAlignment: widget.axisAlignment,
        child: widget.child,
      ),
    );
  }
}
```

### 组件参数说明

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| child | Widget | 必填 | 要应用大小变化过渡动画的子组件 |
| axis | Axis | Axis.vertical | 动画方向，垂直或水平 |
| axisAlignment | double | 0.0 | 动画对齐方式，-1.0 表示顶部/左侧，1.0 表示底部/右侧 |
| duration | Duration | Duration(milliseconds: 300) | 动画持续时间 |
| curve | Curve | Curves.easeInOut | 动画曲线 |
| onTap | Function()? | null | 点击回调函数 |

### 使用示例

```dart
// 垂直方向大小变化
SizeTransitionWidget(
  child: Container(
    width: 200,
    height: 100,
    decoration: BoxDecoration(
      color: Colors.red,
      borderRadius: BorderRadius.circular(12),
    ),
    child: Center(
      child: Text(
        '点击我',
        style: TextStyle(fontSize: 18, color: Colors.white, fontWeight: FontWeight.bold),
      ),
    ),
  ),
  axis: Axis.vertical,
  axisAlignment: 0.0,
  duration: Duration(milliseconds: 500),
  curve: Curves.bounceInOut,
  onTap: _incrementCounter,
),

// 水平方向大小变化
SizeTransitionWidget(
  child: Container(
    width: 200,
    height: 100,
    decoration: BoxDecoration(
      color: Colors.green,
      borderRadius: BorderRadius.circular(12),
    ),
    child: Center(
      child: Text(
        '点击我',
        style: TextStyle(fontSize: 18, color: Colors.white, fontWeight: FontWeight.bold),
      ),
    ),
  ),
  axis: Axis.horizontal,
  axisAlignment: 0.0,
  duration: Duration(milliseconds: 400),
  curve: Curves.elasticOut,
  onTap: _incrementCounter,
),
```

## 首页集成实现

### 集成代码

```dart
import 'package:flutter/material.dart';
import 'components/size_transition.dart';

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter for openHarmony',
      theme: ThemeData(
        colorScheme: ColorScheme.fromSeed(seedColor: Colors.deepPurple),
        useMaterial3: true,
      ),
      debugShowCheckedModeBanner: false,
      home: const MyHomePage(title: 'Flutter for openHarmony'),
    );
  }
}

class MyHomePage extends StatefulWidget {
  const MyHomePage({super.key, required this.title});

  final String title;

  @override
  State<MyHomePage> createState() => _MyHomePageState();
}

class _MyHomePageState extends State<MyHomePage> {
  int _counter = 0;

  void _incrementCounter() {
    setState(() {
      _counter++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flutter for OpenHarmony 实战'),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: <Widget>[
            Text(
              '大小变化过渡动画示例',
              style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 40),

            // 垂直方向大小变化
            SizeTransitionWidget(
              child: Container(
                width: 200,
                height: 100,
                decoration: BoxDecoration(
                  color: Colors.red,
                  borderRadius: BorderRadius.circular(12),
                ),
                child: Center(
                  child: Text(
                    '点击我',
                    style: TextStyle(fontSize: 18, color: Colors.white, fontWeight: FontWeight.bold),
                  ),
                ),
              ),
              axis: Axis.vertical,
              axisAlignment: 0.0,
              duration: Duration(milliseconds: 500),
              curve: Curves.bounceInOut,
              onTap: _incrementCounter,
            ),
            SizedBox(height: 60),

            // 水平方向大小变化
            SizeTransitionWidget(
              child: Container(
                width: 200,
                height: 100,
                decoration: BoxDecoration(
                  color: Colors.green,
                  borderRadius: BorderRadius.circular(12),
                ),
                child: Center(
                  child: Text(
                    '点击我',
                    style: TextStyle(fontSize: 18, color: Colors.white, fontWeight: FontWeight.bold),
                  ),
                ),
              ),
              axis: Axis.horizontal,
              axisAlignment: 0.0,
              duration: Duration(milliseconds: 400),
              curve: Curves.elasticOut,
              onTap: _incrementCounter,
            ),
            SizedBox(height: 30),

            // 点击计数显示
            Text(
              '点击计数: $_counter',
              style: TextStyle(fontSize: 18, fontWeight: FontWeight.bold),
            ),
            SizedBox(height: 20),

            // 提示信息
            Text(
              '提示：点击彩色方块查看大小变化过渡效果',
              style: TextStyle(fontSize: 16, color: Colors.grey),
            ),
          ],
        ),
      ),
    );
  }
}
```

### 实现效果

首页集成了两个不同风格的大小变化过渡动画示例：

1. **红色方块**：使用 `bounceInOut` 曲线的垂直方向大小变化动画，点击时会产生弹跳效果
2. **绿色方块**：使用 `elasticOut` 曲线的水平方向大小变化动画，点击时会产生弹性效果

同时添加了点击计数功能，每次点击动画方块都会增加计数，增强了用户交互体验。

# 开发中容易遇到的问题

## 1. 动画方向与对齐问题

### 问题描述
在设置不同的 `axis` 和 `axisAlignment` 值时，可能会出现动画效果不符合预期的情况。

### 解决方案
- 理解 `axisAlignment` 参数的含义：-1.0 表示顶部/左侧，0.0 表示居中，1.0 表示底部/右侧
- 根据实际需求选择合适的动画方向和对齐方式
- 对于复杂布局，建议先在简单场景中测试动画效果

```dart
// 垂直方向从底部开始动画
SizeTransitionWidget(
  // 其他参数
  axis: Axis.vertical,
  axisAlignment: 1.0, // 从底部开始动画
  // 其他参数
);
```

## 2. 动画控制器生命周期管理问题

### 问题描述
如果不正确管理 `AnimationController` 的生命周期，可能会导致内存泄漏或动画异常。

### 解决方案
- 确保在 `dispose()` 方法中调用 `_controller.dispose()` 释放资源
- 使用 `SingleTickerProviderStateMixin` 为动画控制器提供正确的 vsync
- 避免在动画执行过程中频繁创建或销毁动画控制器

## 3. 性能优化问题

### 问题描述
在复杂布局中使用多个大小变化过渡动画组件时，可能会影响应用性能。

### 解决方案
- 合理设置动画持续时间，避免过长的动画
- 对于不需要交互的动画，考虑使用 `AnimationController` 手动控制动画生命周期
- 当组件不可见时，暂停或取消动画
- 对于复杂的子组件，考虑使用 `RepaintBoundary` 优化重绘性能

## 4. 跨平台兼容性问题

### 问题描述
在不同平台上，大小变化过渡动画的效果可能会有细微差异。

### 解决方案
- 在不同平台上测试动画效果，调整参数以达到最佳视觉效果
- 考虑使用平台特定的动画实现，如在 iOS 上使用 `UIView.animate`，在 Android 上使用 `ViewPropertyAnimator`

# 总结开发中用到的技术点

## 1. Flutter 大小变化过渡动画系统

- **SizeTransition**：Flutter 内置的大小变化过渡动画组件，用于实现平滑的大小变化效果
- **AnimationController**：用于控制动画的播放、暂停、反转等操作
- **CurvedAnimation**：用于为动画添加不同的曲线效果
- **动画曲线**：使用不同的 `Curve` 实现多样化的动画效果
- **动画持续时间**：通过 `Duration` 控制动画的快慢

## 2. 组件化开发

- **自定义组件**：创建可复用的 `SizeTransitionWidget` 组件
- **参数化设计**：通过构造函数参数实现组件的灵活配置
- **状态管理**：使用 `StatefulWidget` 和 `setState` 管理组件内部状态
- **生命周期管理**：正确管理动画控制器的生命周期，避免内存泄漏

## 3. 手势交互

- **GestureDetector**：用于检测用户点击事件，实现交互功能
- **回调函数**：通过 `onTap` 参数实现组件与外部的通信

## 4. 布局设计

- **Flex 布局**：使用 `Column` 和 `SizedBox` 实现垂直布局
- **居中对齐**：使用 `Center` 和 `mainAxisAlignment` 实现内容居中
- **圆角效果**：使用 `BorderRadius` 实现组件的圆角效果
- **动画对齐**：通过 `axisAlignment` 控制动画的对齐方式

## 5. Flutter for OpenHarmony 适配

- **跨平台兼容**：代码同时支持 Flutter 和 OpenHarmony 平台
- **资源管理**：遵循 Flutter 资源管理最佳实践
- **性能优化**：针对 OpenHarmony 平台进行合理的性能优化

## 6. 代码组织

- **目录结构**：按照功能模块化组织代码
- **命名规范**：遵循 Flutter 命名规范，提高代码可读性
- **注释规范**：添加清晰的代码注释，便于维护和理解

通过本次开发，我们实现了一个功能完整、交互友好的大小变化过渡动画效果，展示了 Flutter for OpenHarmony 跨平台开发的优势。该实现不仅满足了基本的动画需求，还通过组件化设计提高了代码的可复用性和可维护性。


欢迎加入开源鸿蒙跨平台社区： https://openharmonycrossplatform.csdn.net
